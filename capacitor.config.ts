import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'rs.ivamr.carservicetracker',
  appName: 'car-service-tracker',
  webDir: 'www'
};

export default config;
